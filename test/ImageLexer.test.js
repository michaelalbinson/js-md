'use strict';

const { describe, it } = require('mocha');

describe('ImageLexer', () => {
	describe('#isTrigger', () => {
		it('should pass', () => {

		});
	});

	describe('#lex', () => {
		it('should pass', () => {

		});
	});
});