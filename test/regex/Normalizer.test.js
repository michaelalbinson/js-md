'use strict';

const { describe, it } = require('mocha');
const { expect } = require('chai');
const Normalizer = require('../../src/regexp/Normalizer');


describe('Normalizer', () => {
    describe('#normalize', () => {
        it('should pass', () => {

        });
    });
});
