'use strict';

const { describe, it } = require('mocha');
const { expect } = require('chai');
const RLexer = require('../src/RLexer');


describe('RLexer', () => {
    describe('', () => {
        it('should pass', () => {

        });
    });
});
